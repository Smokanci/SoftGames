#!/bin/sh
# Strip Co-Authored-By trailers added by AI assistants.
# Lives in hooks/ (not .git/hooks/) because core.hooksPath=hooks makes git
# ignore .git/hooks entirely — a copy left there is dead weight.
sed -i.bak '/^Co-Authored-By:/d' "$1"
rm -f "$1.bak"
